# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'less', :all_on_start => true, :all_after_change => true, :output => 'stylesheets', :import_paths => ['_less'] do 
  watch(%r{^_less/(bootstrap.less)$})
end
